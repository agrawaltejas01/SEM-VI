1.Open a csv file
data = read.csv("cleevland.csv", header = TRUE, sep = ",", dec = ".")
print data

2. Create a subset of X0 > 0
info <- subset(data,X0>0)
> print (info)

3. Take count of (2)
print(nrow(info))


4. Subset in 3 according to age

Young = subset(data,X63.0<=30)
Adult = subset(data,X63.0>30 & X63.0<=45)
Old = subset(data,X63.0>45)
